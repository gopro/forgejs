/**
 * @typedef {{yaw:number, pitch:number, roll:number}}
 * @name TEuler
 * @property {number} yaw - yaw euler angle (y axis) [radians]
 * @property {number} pitch - pitch euler angle (x axis) [radians]
 * @property {number} roll - roll euler angle (z axis) [radians]
 */
var TEuler;

/**
 * @typedef {{x:number, y:number, z:number}}
 */
var CartesianCoordinates;
