{
    "id": "hotspot-states",
    "$schema": "http://json-schema.org/draft-04/schema#",
    "title": "HotspotStatesConfig",
    "description": "An instance of <a href='#hotspot-states'>the hotspot states object.</a>",
    "type": "object",

    "properties": {

        "default": {
            "type": "string",
            "title": "Default",
            "description": "The default state name to load",
            "default": "default"
        },

        "auto": {
            "type": "boolean",
            "title": "Auto",
            "description": "Does the hotspot auto switch to the appropriate state?",
            "default": true
        },

        "over": {
            "$ref": "hotspot-state"
        }
    }
}